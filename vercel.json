{
  "buildCommand": "cd frontend && npm install && npm run build",
  "outputDirectory": "frontend/dist",
  "installCommand": "cd frontend && npm install --no-optional",
  "framework": null,
  "env": {
    "VITE_API_URL": "https://stockai-backend.onrender.com/api/v1"
  }
}